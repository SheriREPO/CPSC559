json.dumps() — Python dict → JSON string dumps = dump to string
json.loads() — JSON string → Python dict


[self._send_to(pid, payload) for pid in SERVER_IDS if pid != self.id]

result = [expression for item in collection if condition]

*tasks       →  unpacks a LIST into individual items
**payload    →  unpacks a DICT into individual key=value pairs